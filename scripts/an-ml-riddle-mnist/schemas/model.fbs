include "common.fbs";

namespace anmlriddle;

file_identifier "AMRM";
file_extension "amrm";

table DenseLayer {
  weights:Matrix;
  biases:[int16];
}

union Layer { DenseLayer }

table LayerTable {
  layer:Layer;
}

table Model {
  layers:[LayerTable];
}

root_type Model;
